{
  "$schema": "https://turborepo.dev/schema.json",
  "globalEnv": [
    "PORT",
    "SOCKET_URL",
    "REDIS_HOST",
    "REDIS_PORT",
    "NEXTAUTH_URL",
    "DATABASE_URL",
    "REDIS_PASSWORD",
    "NEXTAUTH_SECRET"
  ],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        "packages/db-config/prisma/schema.prisma"
      ],
      "outputs": [
        "dist/**",
        ".next/**",
        "!.next/cache/**",
        "packages/db-config/prisma/migrations/**"
      ]
    },
    "lint": {
      "cache": false,
      "dependsOn": [
        "^lint"
      ]
    },
    "check-types": {
      "dependsOn": [
        "^check-types"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": [
        ".env"
      ]
    }
  }
}